# macOS
.DS_Store

# Local environment overrides
.env
.env.local

# Editor / IDE settings
.idea/
.vscode/

# Editor backups
*.swp
*~

# Claude Code local settings
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv writes this whenever a pyproject.toml is present, and here it locks
# nothing: the repository declares no dependencies of its own, and every
# helper resolves its own from its PEP 723 metadata.
uv.lock
